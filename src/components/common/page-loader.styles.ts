import { createStyles, makeStyles } from "@material-ui/core/styles";

export default makeStyles(
	createStyles({
		container: {
			position: "absolute",
			top: 0,
			left: 0,
			right: 0,
			bottom: 0,
		},
	}),
);
